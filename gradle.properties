kotlin.stdlib.default.dependency=false
# IntelliJ Platform Gradle Plugin 2.11.0 tasks are not configuration-cache compatible.
# Keep the cache disabled so `buildPlugin` works from both IDEA and the Gradle wrapper.
org.gradle.configuration-cache=false
org.gradle.caching=true
